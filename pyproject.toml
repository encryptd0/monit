[project]
name = "monit"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Riekus", email = "riekusgroblerps4@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "psutil>=7.2.2",
]

[project.scripts]
monit = "monit:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
